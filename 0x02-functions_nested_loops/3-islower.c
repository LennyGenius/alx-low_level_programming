#include "main.h"
/**
 * islower - Return 1 if letter is lowercase, 0 if not.
 * @c: char type letter
 * Return: 1 if lowercase, 0 if not lowercase
 */
int islower(int c)
{
	if (r > 96 && r < 123)
		return (1);
	else
		return (0);
}
